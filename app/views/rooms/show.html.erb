<h1 class="text-center text-4xl my-12">It's gonna be..... Wait for it !!! Corodinary 🦇</h1>

<p class="text-center text-blue-600">Fais tourner a tes potes, ils vont arriver 🚀 => <%= room_url(@room.token) %></p>

<div class="flex justify-center items-center">
  <button id="start" class="<%= 'hidden' if @room.players.present? %> border border-black border-solid px-4 py-2 mt-8 hover:bg-gray-200 mx-4">Commencer 🎬</button>
  <button id="refresh" class="border border-black border-solid px-4 py-2 mt-8 hover:bg-gray-200 mx-4">Rafraichir 🔥</button>
</div>

<div id="participants" class="mx-auto mt-8" data-token="<%= @room.token %>" data-current-user="<%= current_user %>"></div>

