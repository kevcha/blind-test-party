<!DOCTYPE html>
<html>
  <head>
    <title>BlindTestParty</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application' %>
  </head>

  <body class="<%= params[:debug].present? ? 'debug': '' %>">
    <%= yield %>
    <%= javascript_pack_tag 'application' %>
  </body>
</html>
